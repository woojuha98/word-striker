# 저장소에는 항상 LF로 담고, 체크아웃할 때도 LF를 유지한다.
# 개발은 Windows에서 하지만 배포 빌드는 Vercel(Linux)에서 돌아가므로,
# 줄바꿈이 플랫폼마다 달라지면 diff가 통째로 흔들린다.
* text=auto eol=lf

# 바이너리 — 줄바꿈 변환 대상이 아니다
*.png binary
*.svg text eol=lf
*.mp3 binary
*.wav binary
