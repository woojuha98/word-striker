# WORD STRIKER — 게임 설계 문서

> 스포츠 × 영어 단어 학습 모바일 게임
> 최종 수정: 2026-08-13

---

## 0. 이 문서를 읽는 Claude Code에게

- **§2 (v1 범위)에 있는 것만 구현한다.** §3 이후는 로드맵이며, 요청 전까지 구현하지 않는다.
- 다만 **§3의 확장을 염두에 둔 구조**로 작성한다. 특히:
  - 문제 유형(`EN_KO`, `KO_EN`, `CLOZE`)이 나중에 추가될 수 있도록 문제 생성 로직을 유형별로 분리
  - 종목(축구/야구/농구)이 추가될 수 있도록 게임 씬을 공통 인터페이스로 추상화
  - 점수 계산 로직은 UI와 완전히 분리된 순수 함수로 작성
- **한 번에 하나의 기능만** 구현하고 동작을 확인한 뒤 다음으로 넘어간다.
- 점수 산식·콤보 규칙(§5)은 **밸런싱의 핵심이므로 임의로 변경하지 않는다.**

---

## 1. 프로젝트 개요

### 1.1 컨셉

영어 단어 학습을 스포츠 경기 형식으로 변환한 게이미피케이션 앱.
플레이어는 여러 종목의 경기를 치르고, 총점에 따라 금·은·동메달을 획득한다.

### 1.2 목표

- 게임사 지원용 **포트폴리오**
- 어휘 팩 판매를 통한 **개인 수익 창출**

### 1.3 설계 원칙

| 원칙 | 의미 |
|---|---|
| 학습 평가의 순수성 | 조작 실패나 연출 요소가 점수에 영향을 주지 않는다 |
| 회피보다 시도 | 모르는 문제를 넘기는 것보다 틀리더라도 시도하는 쪽이 유리하다 |
| 하위권 이탈 방지 | 실수 한 번으로 판 전체가 무의미해지지 않는다 |
| 완성도 > 분량 | 종목 2개를 매끄럽게 > 종목 6개를 어설프게 |

### 1.4 기술 스택

- **React + TypeScript + Vite**
- **Tailwind CSS** — 스타일
- **Framer Motion** — 공 궤적, 콤보 게이지 애니메이션
- **Zustand** — 게임 상태 관리
- 배포: **Vercel**
- (이후) **Supabase** — 인증·기록·리더보드
- (이후) **Capacitor** — 앱스토어 패키징

---

## 2. v1 범위 — 이것만 만든다

| 항목 | 스펙 |
|---|---|
| 종목 | 축구 PK **1종목** |
| 문제 유형 | `EN_KO` (영어 → 한국어 뜻 4지선다) **1종** |
| 어휘 | 교육과정 기본어휘 **30개**, JSON 하드코딩 |
| 조작 | 4분할 골대 중 하나를 **탭** |
| 파워 게이지 | **없음** (v1.1에서 추가) |
| 시간 제한 | 문제당 **5초** |
| 한 판 | **10문제** |
| 결과 | 총점, 정답 개수, 최고 콤보 표시 |
| 저장 | `localStorage`에 최고 점수만 |

### v1에 없는 것

로그인 · 서버 · DB · 결제 · 광고 · 메달 시스템 · 난이도 등급 · 사운드 · 리더보드

---

## 3. 로드맵

| 순서 | 내용 | 상태 |
|---|---|---|
| v1.0 | 축구 PK · `EN_KO` · 탭 조작 (§2) | ✅ 완료 |
| v1.1 | 파워 게이지 + 골키퍼 반응 + 튜토리얼 + 오답 리뷰 (§13) | ✅ 완료 |
| v1.2 | 사운드 + 애니메이션 폴리싱 (§14) | ⏸ 음원 대기 중 |
| v1.3 | `KO_EN` 문제 유형 추가 | ✅ 완료 |
| v1.4 | `CLOZE` 문제 유형 추가 | ✅ 완료 |
| v2.0 | 난이도 등급 3단계 (초·중·고급) | ✅ 완료 |
| — | SVG 교체 (§12.5) | 다음 |
| — | Vercel 배포 → 공유 링크 확보 | 다음 |
| v2.1 | 야구 종목 추가 (§9) | |
| v2.2 | 6종목 + 메달 시스템 (§8) | |
| v3.0 | Supabase 리더보드 | |
| v3.1 | 어휘 팩 결제 (§11) | |

**v1.2 잔여 작업**: 음원 3개(`correct.mp3` / `wrong.mp3` / `fanfare.mp3`)를 `public/sounds/`에 배치하면 Howler 연결·iOS 언락·콤보 음정 상승·음소거 토글이 이어진다. 나머지 6종은 Web Audio 합성으로 처리한다.

**SVG 교체**: 보류 승인됨. v1.1에서 입력·연출 타이밍이 확정되었으므로 착수 가능하다.

---

## 4. 화면 설계

### 4.1 레이아웃 (세로 9:16 기준)

```
┌─────────────────────────┐
│  점수 1,240    ×1.5 🔥  │  상단 HUD          10%
├─────────────────────────┤
│   ⏱ ████████░░░  3.2s   │  타이머 바          5%
├─────────────────────────┤
│                         │
│        achieve          │  문제 영역         15%
│                         │
├─────────────────────────┤
│ ┌───────────┬─────────┐ │
│ │  달성하다  │ 포기하다 │ │
│ │           │   🧤    │ │  골대 4분할        45%
│ ├───────────┼─────────┤ │  (화면 폭 가득)
│ │  미루다    │ 제안하다 │ │
│ └───────────┴─────────┘ │
├─────────────────────────┤
│           ⚽            │  공 + 키커         20%
│           👤            │
└─────────────────────────┘
```

### 4.2 골대 영역 규칙

- **폭**: 화면 좌우 여백 8px씩만 두고 가득 채운다
- **분할**: 2×2 균등, 칸 경계는 골대 그물 선으로 표현
- **폰트 자동 축소**: 기본 20px → 넘칠 경우 14px까지 단계적 축소
- **골키퍼**: 골대 중앙에 배치. 4칸 중 어느 칸과도 완전히 겹치지 않을 것
- **탭 판정**: 칸 전체가 터치 영역. 최소 44×44pt 이상 보장

### 4.3 공 / 키커 영역

- v1에서는 **정적 그림**
- 탭 시 공이 선택한 칸으로 날아가는 애니메이션만 구현
- 키커의 슛 모션은 v1.2 이후

### 4.4 화면 흐름

```
[시작 화면] → [게임 화면] × 10문제 → [결과 화면] → 다시하기 / 시작 화면
```

---

## 5. 점수 시스템 ★핵심

### 5.1 산식

```
종목 원점수 = Σ(정답 기본점 × 콤보 배수) − Σ(오답 패널티 + 시간초과 패널티)
```

**하한 0은 매 문제 정산 시점에 누적 점수에 적용한다.** (최종 합계에만 적용하는 것이 아니다)

- 즉 화면의 점수가 어느 순간에도 음수로 내려가지 않는다
- 0에서 패널티를 받으면 0에 머문다
- 근거: 판 중반에 음수 점수가 표시되면 "이번 판은 끝났다"는 신호가 되어 남은 문제를 포기하게 된다. 하위권 이탈 방지가 최종 정산의 엄밀함보다 우선한다

### 5.2 기본 점수

| 상황 | 점수 |
|---|---|
| 정답 | **+100** (× 콤보 배수) |
| 오답 (잘못된 칸을 탭) | **−50** |
| 시간초과 (제한 시간 내 미입력) | **−30** |

> **오답 패널티 > 시간초과 패널티인 이유**
> 반대로 설계하면 "모르는 문제는 그냥 흘려보내기"가 최적 전략이 되어 인출 시도 자체를 하지 않게 된다.
> 시도했다가 틀리는 편이 학습에 유익하므로 무행동을 더 관대하게 처벌하되, 완전 무료로 두지는 않는다.

### 5.3 콤보 배수

| 단계 | 조건 | 배수 |
|---|---|---|
| 0 | — | ×1.0 |
| 1 | 3연속 정답 | ×1.2 |
| 2 | 5연속 정답 | ×1.5 |
| 3 | 8연속 정답 | ×2.0 **(상한)** |

**하락 규칙**
- 오답 또는 시간초과 시 **한 단계만 하락** (×2.0 → ×1.5)
- **즉시 복귀형**: 하락 후 정답 1개를 맞히면 곧바로 이전 단계로 복귀
- 상한 ×2.0을 반드시 지킬 것 — 무한 상승 시 메달 기준(§8)이 무의미해진다

> **완전 초기화를 쓰지 않는 이유**
> 초반 실수 한 번으로 그 판 전체가 무의미해지는 느낌을 주며, 이는 세션 중도 이탈의 주요 원인이다.

### 5.3.1 배수 적용 시점 (확정)

**단계를 올린 그 정답부터 새 배수를 적용한다.** (다음 문제부터가 아니다)

- 3연속을 완성한 3번째 정답이 곧바로 `100 × 1.2 = 120`점을 받는다
- **즉시 복귀에도 동일하게 적용**한다. ×2.0에서 하락해 ×1.5가 된 뒤 맞힌 정답은 그 자리에서 ×2.0으로 계산된다
- 근거: 콤보 상승 연출(§12.4 플래시)과 획득 점수 플로팅(§14.7)이 같은 순간에 터져 하나의 보상으로 묶인다. 다음 문제로 미루면 "무엇 때문에 점수가 올랐는지" 인과가 끊긴다

### 5.3.2 내부 상태 분리 (필수)

연속 정답 카운터를 **두 개로 분리한다.** 하나로 겸용하면 즉시 복귀 시 값이 오염된다.

| 변수 | 용도 | 즉시 복귀 시 |
|---|---|---|
| `progress` | 단계 판정용 | 복귀에 필요한 요건까지 끌어올림 |
| `streak` | 실제 연속 정답 수, 결과 화면 "최고 콤보" 표시용 | 실제 값만 증가 |

> 겸용 시 실제 4연속인데 결과 화면에 6으로 표시되는 결함이 발생한다.

### 5.4 콤보 시각 피드백

- 콤보는 숫자보다 **스포츠 은유**로 표현: 관중 함성 볼륨, 배경 조명, 이펙트 강도
- **하락 시 게이지가 0으로 뚝 떨어지는 연출을 쓰지 말 것.** 심리적으로 완전 초기화처럼 느껴진다. 반드시 "한 칸 내려옴"으로 보이게 한다

---

## 6. 문제 유형

### 6.1 유형 목록

| 코드 | 유형 | 제시 | 골문 선택지 | 시간 제한 |
|---|---|---|---|---|
| `EN_KO` | 영→한 | achieve | 달성하다 / 포기하다 / … | **4초** |
| `KO_EN` | 한→영 | 달성하다 | achieve / abandon / … | **5초** |
| `CLOZE` | 빈칸 | She finally ___ her goal. | achieve / … | **8초** |

> v1에서는 `EN_KO`만 구현. 단, 시간 제한은 v1 단순화를 위해 **5초 고정**으로 시작한다.
> 유형이 추가되는 v1.3 시점에 위 표대로 유형별 차등을 적용한다.

### 6.2 등급별 출현 비율 (v2.0에서 적용)

| 등급 | EN_KO | KO_EN | CLOZE |
|---|---|---|---|
| 초급 | 65% | 35% | — |
| 중급 | 35% | 45% | 20% |
| 고급 | 20% | 40% | 40% |

- 초급 등급은 모든 시간 제한에 **×1.3** 적용
- 비율은 상수로 분리하여 밸런싱 시 숫자만 수정 가능하게 할 것

### 6.3 구현 원칙

- `example` 필드가 비어 있는 단어는 `CLOZE` 출제 대상에서 **자동 제외**
- 문제 유형은 확장 가능한 형태로 분리 (유형별 생성 함수 + 공통 인터페이스)
- 한 판 안에서 같은 단어가 두 번 나오지 않을 것

---

## 7. 조작 방식

### 7.1 v1 — 탭

1. 문제 제시 → 5초 카운트다운 시작
2. 4분할 골대 중 하나를 탭
3. 공이 해당 칸으로 날아감 → 정답/오답 판정 → 결과 표시

### 7.2 v1.1 — 탭 + 파워 게이지

**입력 흐름**

1. 문제 제시 (카운트다운 시작)
2. 골대 칸 하나를 **누른 채 유지** → 그 순간 파워 게이지 왕복 시작
3. 손을 뗌 → 게이지 정지, 구간 판정
4. 슛 연출 → 결과 표시

**게이지 스펙**

- 왕복 주기: **1.2초** (좌 → 우 → 좌)
- 초록 구간: 중앙 기준 전체 폭의 **30%**
- **골대를 누른 시점에 문제 타이머는 정지**한다 (게이지 조작 시간은 제한 시간에 포함되지 않음)

> 이 규칙이 없으면 "단어는 아는데 게이지 맞추다 시간초과"가 발생하여 학습 평가가 오염된다.

**결과 매트릭스**

| 탭한 칸 | 게이지 | 연출 | 점수 |
|---|---|---|---|
| 정답 | 초록 | **골!** 환호 | +100 × 배수, 콤보 상승 |
| 정답 | 빨강 | **선방!** 아쉬움 | +100 × 배수, 콤보 상승 |
| 오답 | 초록 | 골 + 오답 표시 | −50, 콤보 하락 |
| 오답 | 빨강 | 선방 + 오답 표시 | −50, 콤보 하락 |

> **파워 게이지는 순수 연출이며 점수에 일절 영향을 주지 않는다.**
> 스포츠 게임의 손맛만 더하고 학습 평가는 건드리지 않기 위함이다.

### 7.3 예외 처리

| 상황 | 처리 |
|---|---|
| 제한 시간 내 미입력 | 시간초과, −30 |
| 누른 채 3초 이상 유지 (v1.1) | 자동 발사 |
| 칸을 누른 뒤 밖으로 드래그해서 뗌 | **취소 없이 그대로 발사** |

> 취소를 허용하면 "잘못 눌렀을 때 무르기"가 가능해져 시간 압박이 무의미해진다.

### 7.4 사용하지 않는 입력

스와이프 · 드래그 · 멀티터치 · 기울기 센서 — 전 종목이 **탭 하나로 통일**된다.

---

## 8. 메달 시스템 (v2.2)

### 8.1 구조

6종목을 모두 완주 → 각 종목 원점수를 **1000점 만점으로 환산** → 총합 6000점 만점 → 메달 판정

### 8.2 고정 기준 (보정하지 않는다)

| 등급 | 총점 |
|---|---|
| 🥇 금메달 | 5,000 이상 |
| 🥈 은메달 | 4,000 이상 |
| 🥉 동메달 | 3,000 이상 |
| 입상 | 2,000 이상 |
| 예선 통과 | 완주 |

- **"순위권 밖"이라는 표현을 쓰지 않는다.** 완주만 해도 "예선 통과"로 표기한다
- 기준을 수정해야 할 경우 패치가 아닌 **"시즌 규정 변경"**으로 처리하고 이전 기록은 시즌별로 보존한다

> **고정 기준을 쓰는 이유**
> 기준이 움직이면 "다음엔 얼마나 더 해야 하는가"를 계산할 수 없다. 구체적이고 도달 가능한 목표가 성립하려면 기준이 고정되어야 한다.

### 8.3 난이도 등급 분리

고정 기준의 약점(초급 학습자가 금메달에 도달 불가)은 기준을 흔들어서가 아니라 **경기 자체를 나눠서** 해결한다.

- 초급 / 중급 / 고급 = 출제 어휘 등급이 다른 **별개의 대회**
- 각 등급 내에서는 **동일한 고정 기준** 적용
- 등급별 메달은 시각적으로 구분 (색·테두리)

### 8.4 약점 종목 회피 방지

- **종목별 최소 커트라인**: 한 종목이라도 300점 미만이면 메달 등급 1단계 하락
- **개인 기록 갱신 보너스**: 절대 점수가 낮아도 자기 최고 기록을 경신하면 가산점

### 8.5 세션 구조

| 모드 | 내용 |
|---|---|
| 일상 모드 | 매일 2~3종목, 짧게 |
| 종합 대회 | 주 1회 전 종목 완주 → 메달 판정 |

> 메달 판정을 주 1회로 제한하여 희소성을 확보한다.

---

## 9. 종목 확장 계획

각 종목은 **서로 다른 인지 과제**를 담당한다. 종목 추가 = 학습 단계 추가.

| 종목 | 메커닉 | 인지 과제 | 우선순위 |
|---|---|---|---|
| ⚽ 축구 PK | 4분할 골대 탭 | 재인 (recognition) | **v1** |
| ⚾ 야구 | 칠 공 / 거를 공 판단 | 변별, 억제 통제 | v2.1 |
| 🏀 농구 | 철자 타일 순서대로 탭 | 인출 (recall) | 이후 |
| 🏹 양궁 | 조준 정밀도 | 문맥상 최적어 선택 | 이후 |
| 🏃 육상 | 연속 정답 = 속도 | 유창성, 자동화 | 이후 |
| 🎳 볼링 | 프레임 누적 | 간격 반복 복습 | 이후 |

### 종목 간 점수 정규화

- 각 종목 원점수를 **1000점 만점으로 환산**한 뒤 합산
- 환산식은 실사용 데이터 분포(백분위)를 참고해 확정하고, 이후 **고정**
- 정규화 없이 합산하면 특정 종목만 파는 편법 루트가 생기고, 학습자가 약한 과제 유형을 회피하게 된다

---

## 10. 데이터 구조

### 10.1 v1 (단순형)

```json
{
  "id": "w001",
  "en": "achieve",
  "ko": "달성하다",
  "distractors": ["포기하다", "미루다", "제안하다"]
}
```

### 10.2 확장형 (v1.3 이후)

```json
{
  "id": "w001",
  "en": "achieve",
  "ko": "달성하다",
  "level": "basic",
  "example": "She finally ___ her goal.",
  "distractors": {
    "ko": ["포기하다", "미루다", "제안하다"],
    "en": ["abandon", "postpone", "suggest"]
  }
}
```

- **⚠ 정정**: `distractors`가 배열 → 객체로 바뀌는 것은 **필드 추가가 아니라 형태 변경(breaking change)**이다. 초판 문서의 "필드만 추가하므로 안 깨진다"는 설명은 오류였다
- 대응: 두 형태를 모두 받는 **유니온 타입 + `distractorsFor(word, lang)` 접근자**로 처리한다. 호출부는 접근자만 사용하고 원본 형태를 직접 참조하지 않는다
- `level`, `example`은 선택적 필드(`level?`, `example?`)로 두면 실제로 무해하게 추가된다

### 10.3 v1 데이터셋 (작성 완료)

`src/data/words.json` — 30개 확정.
구성: **동사 12 / 명사 10 / 형용사 8**. 품사를 섞어 10문제 출제 시 화면이 단조로워지지 않게 한다.

### 10.4 오답 선택지 작성 원칙

- 오답은 **미리 지정**한다 (랜덤 추출 금지)
- 한국어 뜻은 **5~7자 이내**로 다듬는다 (골대 칸 크기 제약)
- 오답 3개는 아래 세 갈래를 섞어 구성한다:

| 갈래 | 예 | 효과 |
|---|---|---|
| **반의어** | reduce 줄이다 ↔ 늘리다 | 가장 강력. 어렴풋한 이해로는 거를 수 없다 |
| **혼동어** | borrow 빌리다 ↔ 빌려주다 | 학습자가 실제로 자주 헷갈리는 쌍 |
| **동일 범주** | bridge 다리 ↔ 터널·도로·계단 | 소거법 차단 |

- 무관한 단어만 넣으면 소거법으로 풀려 변별력이 사라진다

### 10.5 CLOZE 예문 작성 규칙

- **자체 작성.** 기존 교재·사전 예문을 그대로 쓰지 않는다 (§10.7 저작권)
- 문장 길이 **8단어 이내** — 8초 제한 안에 읽고 판단해야 한다
- 빈칸 외의 단어는 해당 어휘 팩 수준을 넘지 않는다
- **문맥만으로 정답이 특정되어야 한다.** 오답 3개 중 하나를 넣어도 말이 되면 실격
- **예문에 오답 선택지 단어가 등장해서는 안 된다.** 문맥에 힌트가 새거나 문장이 어색해진다
- 마땅한 예문이 안 나오면 `example`을 **비워둔다.** §6.3에 따라 CLOZE에서 자동 제외된다. 억지로 채우지 않는다
- 초급 등급은 CLOZE 출현 비율이 0%이므로(§6.2) 예문이 필요 없다

> 구체명사는 쉽지만 `similar`, `useful` 같은 형용사는 문맥으로 하나를 특정하기 어렵다. 일부가 비는 것이 정상이다.

### 10.6 다의어 처리 — `koPrompt` (확정)

`KO_EN`은 한국어를 제시하고 영어를 고르게 하므로, 제시어가 다의어면 학습자가 다른 영어 단어를 먼저 떠올린다.

**해법**: 선택적 필드 `koPrompt`를 두고 **`KO_EN`에서만** 사용한다. `EN_KO`·`CLOZE`는 기존 `ko`를 그대로 쓴다.

- 표기: `약(먹는 것)` 형태. **괄호 안 6자 이내** (골대 칸 폭 제약)
- `koPrompt`가 없으면 `ko`로 폴백
- 유의어 관계(기회=opportunity/chance, 목적=purpose/goal 등)에는 **붙이지 않는다.** 다의어가 아니므로 각인이 틀리지 않고, 괄호만 늘어나 화면이 지저분해진다

**현재 적용 대상 (3개)**

| id | koPrompt | 이유 |
|---|---|---|
| w020 | `약(먹는 것)` | 약 10명(about), 약하다(weak)로도 읽힘 |
| w021 | `다리(강 위의)` | leg를 먼저 떠올림 |
| w002 | `빌리다(내가 받는)` | borrow/lend 방향 혼동. 선택지에 `lend`가 있어 실제로 걸린다 |

> 어휘 팩을 확장할 때마다 동일 기준으로 전수 점검한다.

### 10.7 어휘 팩

| 팩 | 상태 |
|---|---|
| 교육과정 기본어휘 | **무료** |
| 수능 영어 | 유료 |
| 토익 | 유료 |
| 토플 | 유료 |

> **저작권 주의**: 단어 자체는 보호 대상이 아니지만, 특정 출판사 단어장의 선별 구성과 예문을 그대로 사용하면 문제가 된다.
> 공개 코퍼스 기반 빈도 리스트에서 직접 추출하고, **예문은 자체 작성**한다.

---

## 11. 수익화 (v3.1)

| 방식 | 정책 |
|---|---|
| 어휘 팩 | 단품 구매 ₩5,900~9,900 + 통합팩 할인 |
| 구독 | **초기 도입 금지** — 지속 콘텐츠 공급 부담이 크다 |
| 광고 | **리워드형만** (콤보 부활, 하트 충전). 배너·전면광고 금지 |

> **무료 구간을 넓게 유지할 것.** 기본어휘만으로 2~3주는 즐길 수 있어야 결제 전환이 일어난다.
> 무료 구간이 짧으면 결제가 아니라 이탈이 발생한다.

---

## 12. 톤앤매너

### 12.1 방향 — 플랫 벡터 + 야간 경기장

**모든 아트를 SVG와 CSS로 그린다. 이미지 에셋 파일을 사용하지 않는다.**

| 근거 | 내용 |
|---|---|
| 제작 부담 제거 | 골대·그물·잔디·공·키커를 전부 코드로 생성 → 에셋 병목이 없다 |
| 콤보 연출과 결합 | 어두운 배경이어야 조명이 밝아지는 연출의 변화 폭이 크다 (§5.4) |
| 가독성 | 어두운 칸 + 흰 글자 조합이 골대 4분할 텍스트에 가장 잘 읽힌다 |

### 12.2 컬러 팔레트

| 요소 | 값 | 비고 |
|---|---|---|
| 배경 (밤하늘·관중석) | `#0B1220` | 최하층 |
| 잔디 | `#14532D` → `#166534` | 아래로 갈수록 밝게 (그라디언트) |
| 골대 프레임 | `#F8FAFC` | |
| 골대 그물 | `#F8FAFC` @ 20% | |
| 골대 칸 기본 | `rgba(255,255,255,0.06)` | 테두리 흰색 1px |
| 정답 | `#22C55E` | |
| 오답 | `#EF4444` | |
| **콤보·점수 (시그니처)** | `#FACC15` | 야간 조명색 |

> Tailwind `theme.extend.colors`에 그대로 등록하여 하드코딩을 피한다.

### 12.3 타이포그래피

- 서체: **Pretendard** 단일 (한글·영문·숫자 모두 대응, 웹폰트 무료)
- 점수 숫자에 `font-variant-numeric: tabular-nums` **필수**
  → 없으면 점수 증가 시 숫자 폭이 변해 HUD가 흔들린다
- 문제 단어 32~40px / 골대 칸 20px (넘칠 경우 14px까지 축소, §4.2)

### 12.4 모션 (Framer Motion)

| 연출 | 시간 | 비고 |
|---|---|---|
| 공 궤적 | 0.35초 | 더 길면 답답, 짧으면 궤적이 안 보임 |
| 골키퍼 다이빙 | 0.25초 | 공보다 살짝 먼저 시작 |
| 콤보 상승 플래시 | 0.2초 | 화면 전체 노란빛 |
| 콤보 하락 | — | **조명이 한 단계 어두워지는 연출.** 게이지 급락 애니메이션 금지 (§5.4) |

### 12.5 v1 단계 지침

- 공·골키퍼·키커는 **이모지(⚽ 🧤 👤)로 시작**한다
- 레이아웃과 모션 타이밍을 먼저 확정한 뒤 SVG로 교체한다
- 사운드는 v1.2까지 넣지 않는다

---

## 13. v1.1 상세 — 파워 게이지 + 부속 작업

파워 게이지 도입은 단독 기능이 아니다. 아래 4개가 한 묶음이다.

### 13.1 파워 게이지

스펙은 §7.2 참조. 입력이 **탭 → 누른 채 유지 후 떼기**로 바뀌므로 기존 탭 핸들러를 전면 교체한다.

### 13.2 골키퍼 반응 로직

게이지 판정 결과에 따라 골키퍼가 다르게 움직인다. 게이지가 도입되기 전에는 존재할 수 없는 로직이므로 함께 구현한다.

| 게이지 | 골키퍼 |
|---|---|
| 초록 | 반대 방향으로 다이빙 → 실점 |
| 빨강 | 슛 방향으로 다이빙 → 선방, 공 리바운드 |

- 다이빙 0.25초, 공보다 살짝 먼저 시작 (§12.4)
- **점수는 게이지와 무관하다.** 골키퍼 연출은 시각적 결과일 뿐이다 (§7.2)

### 13.3 첫 플레이 튜토리얼 ★필수

"누른 채 유지했다가 뗀다"는 조작은 직관적으로 알 수 없다. **게이지 도입과 동시에 반드시 추가한다.**

- 첫 실행 시 1회 노출, `localStorage`로 재노출 방지
- 형식: 정지된 화면 + 손가락 아이콘 애니메이션 + 한 줄 설명
- 첫 문제는 **시간 제한 없이** 진행 (튜토리얼 문제)
- 설명 문구는 두 줄을 넘기지 않는다

> 튜토리얼 없이 게이지를 배포하면 첫 판에서 조작을 이해하지 못해 이탈한다.

### 13.4 결과 화면 오답 리뷰

- 한 판이 끝나면 **틀린 단어와 시간초과 단어를 목록으로** 보여준다
- 각 항목: 영어 단어 / 정답 뜻 / 내가 고른 뜻
- 학습 앱으로서의 실질적 가치가 여기서 나온다. 구현 비용 대비 효과가 가장 큰 기능이다
- v1.4 이후 오답 노트 기능의 기반이 된다

---

## 14. v1.2 상세 — 사운드 + 폴리싱

### 14.1 사운드 정책

**단발 효과음만 사용한다. 배경 루프 트랙(관중 함성 등)을 사용하지 않는다.**

- 관중 환호는 **정답 효과음 안에 포함**시킨다 (네트 흔들림 + 환호가 한 파일)
- 루프 트랙은 파일당 500KB~1MB로 초기 로딩을 눈에 띄게 지연시킨다
- 라이브러리: **Howler.js** (오디오 스프라이트, 모바일 언락, 볼륨 페이드 지원)

### 14.2 iOS 오디오 언락 ★필수

iOS Safari는 사용자 터치 없이 오디오를 재생하지 않는다.

- 시작 화면의 **"경기 시작" 버튼 탭 시점**에 무음 파일을 1회 재생하여 오디오 컨텍스트를 연다
- 이 처리가 없으면 데스크톱에서는 정상이지만 모바일에서 완전 무음이 된다

### 14.3 사운드 세트 (9개)

| 상황 | 사운드 | 길이 |
|---|---|---|
| 문제 등장 | 짧은 휘슬 | 0.3s |
| 골대 탭 | 킥 임팩트 | 0.2s |
| 정답 (골) | 네트 흔들림 **+ 환호** | 1.0s |
| 오답 | 부저 + 탄식 | 0.8s |
| 시간초과 | 종료 휘슬 | 0.5s |
| 콤보 상승 | 상승 차임 | 0.4s |
| 콤보 하락 | 하강 톤 | 0.3s |
| 타이머 1초 이하 | 심박 틱 (반복) | 0.15s |
| 결과 화면 | 경기 종료 팡파레 | 2.0s |

### 14.3.1 정답 + 선방 시 소리 (확정)

§7.2 결과 매트릭스 2행(정답인데 게이지 빨강)은 **점수는 들어가는데 그림은 골키퍼가 막는** 상태다. 여기서 환호가 나면 소리와 화면이 어긋난다.

**원칙: 환호는 골 연출에, 콤보 차임은 점수에 종속된다.**

| 결과 | 재생할 소리 |
|---|---|
| 정답 + 초록 (골) | 정답음(네트+환호) + 콤보 차임 |
| **정답 + 빨강 (선방)** | **글러브 캐치음(합성) + 콤보 차임.** 정답음은 재생하지 않는다 |
| 오답 + 초록 | 부저음 |
| 오답 + 빨강 | 부저음 |

- 글러브 캐치음은 짧은 타격음이므로 **Web Audio 합성**으로 처리한다. 파일 추가 불필요
- 콤보 차임은 §14.4대로 단계별 `rate`를 적용한다. 선방이어도 점수가 들어갔으므로 상승 톤이 정상이다
- 부저음은 게이지와 무관하게 동일하다. 오답은 어떤 연출이든 오답이다

> 이 분리로 "아쉽지만 점수는 얻었다"는 감각이 소리만으로 전달된다. 정답음을 그대로 틀면 화면과 모순되고, 부저를 틀면 학습 피드백이 오염된다.

### 14.4 콤보 음정 상승

정답 효과음을 **콤보 단계마다 재생 속도(`rate`)를 올려** 재생한다. 파일 하나로 상승감을 만든다.

| 콤보 단계 | rate |
|---|---|
| ×1.0 | 1.00 |
| ×1.2 | 1.06 |
| ×1.5 | 1.12 |
| ×2.0 | 1.19 |

### 14.5 음소거 토글 ★필수

- HUD 우상단에 배치, `localStorage`에 상태 저장
- 학습 앱 특성상 지하철·도서관 등 무음 환경 사용 비중이 높다. 없으면 이탈 요인이 된다

### 14.6 사운드 리소스 라이선스

수익화를 전제로 하므로 **CC0(퍼블릭 도메인)만 사용한다.**

| 출처 | 비고 |
|---|---|
| Kenney.nl | 게임용 CC0. 품질이 균일하고 가장 안전 |
| Freesound.org | **CC0 필터 필수.** 기본 검색에는 저작자 표시 요구 파일이 섞여 있다 |
| Pixabay | 자체 라이선스, 상업적 이용 가능 |

`ASSETS.md`에 파일별 출처·라이선스를 기록한다. 스토어 심사 및 분쟁 대응에 필요하다.

### 14.7 추가 애니메이션

| 연출 | 스펙 |
|---|---|
| 점수 카운트업 | 즉시 갱신 대신 0.4초에 걸쳐 증가 |
| 점수 팝 | 갱신 시 `scale 1 → 1.15 → 1` |
| 획득 점수 플로팅 | `+120` 이 골대에서 떠올라 사라짐, 0.8초 |
| 화면 흔들림 | 골 성공 시 3px, 0.15초 |
| 네트 출렁임 | 공이 닿은 지점 기준 파동 |
| 타이머 경고 | 1초 이하부터 빨간색 + 맥박 |

### 14.8 넣지 않는 연출

- **파티클 폭발** — 모바일 프레임 드랍의 주 원인
- **화면 전체 전환 효과** — 문제 간 대기가 늘어 리듬이 끊긴다
- **긴 골 세리머니** — 10문제를 빠르게 도는 게임. 1초를 넘기면 지루해진다

### 14.9 성능 원칙

애니메이션은 **`transform`과 `opacity`만** 사용한다.
`width` / `top` / `left`를 애니메이션하면 매 프레임 레이아웃 재계산이 발생해 저사양 기기에서 버벅인다.

---

## 15. 미결정 사항

향후 논의가 필요한 항목:

- [ ] 게임 정식 명칭 (현재 임시명: Word Striker)
- [ ] 하트(생명) 시스템 도입 여부 → 도입 시 리워드 광고 자리가 생김
- [ ] 오답 노트 / 틀린 단어 재출제 기능 (§13.4 오답 리뷰가 기반)
- [ ] 매일 출석 보상
- [ ] 리더보드 범위 (전체 / 친구 / 주간 리셋)
- [x] ~~다의어 처리~~ → **해결.** §10.6 `koPrompt` 방식으로 확정

---

## 부록 A. 설계 의사결정 기록

포트폴리오 제출 시 이 항목들이 **설계 역량의 근거 자료**가 된다.

| 결정 | 선택 | 근거 |
|---|---|---|
| 오답 −50 > 시간초과 −30 | 무행동을 더 관대하게 | 회피 전략이 최적해가 되는 것을 방지. 인출 실패도 후속 학습을 촉진한다 |
| 콤보 1단계 하락 + 즉시 복귀 | 완전 초기화 대신 | 초반 실수로 판 전체가 무의미해지는 느낌 = 중도 이탈의 주 원인 |
| 콤보 상한 ×2.0 | 무한 상승 금지 | 상한이 없으면 고정 메달 기준이 무의미해진다 |
| 파워 게이지 = 연출 전용 | 점수 미반영 | 조작 실패로 인한 오답은 좌절만 남기고 학습 효과가 없다 |
| 메달 = 고정 기준 | 상대 순위 대신 | 자기참조 피드백. 상대 순위는 하위 40%를 영구 무메달로 만든다 |
| 순위 리더보드는 분리 | 메달과 별도 층 | 경쟁 요소를 원하는 사용자만 소비하도록 |
| 전 종목 탭 조작 통일 | 스와이프 배제 | 종목마다 조작을 새로 배울 필요 없음. 코드도 공통 인터페이스로 |
| 종목 = 인지 과제 유형 | 테마 교체 아님 | 학습 내용이 게임 메커닉의 보상 구조에 통합되어야 효과가 난다 |
| 아트 = 코드 생성 SVG | 이미지 에셋 배제 | 1인 개발에서 아트 병목을 제거. 교체·변형이 코드 수정으로 끝난다 |
| 야간 경기장 배경 | 주간 대신 | 콤보 조명 연출의 변화 폭 확보 + 골대 칸 텍스트 가독성 |
| 오답 선택지 사전 지정 | 랜덤 추출 배제 | 반의어·혼동어를 배치해야 실제 변별이 일어난다. 랜덤은 소거법으로 풀린다 |
| 단발 효과음만 사용 | 배경 루프 배제 | 루프 트랙 1개당 500KB~1MB. 초기 로딩 지연 대비 몰입 이득이 작다 |
| 환호를 정답음에 통합 | 별도 트랙 대신 | 파일 하나로 골 성공의 감각을 완결. 볼륨 페이드 로직도 불필요해진다 |
| 콤보 = 음정 상승 | 사운드 파일 추가 없이 | `rate` 조정만으로 상승감 확보. 구현 비용이 사실상 0 |
| 튜토리얼을 v1.1에 포함 | 게이지와 동시 배포 | 누른 채 유지 후 떼기는 직관적이지 않다. 미설명 시 첫 판에서 이탈한다 |
| 배수를 올린 정답부터 적용 | 다음 문제부터가 아님 | 콤보 플래시와 점수 플로팅이 같은 순간에 터져야 인과가 유지된다 |
| 하한 0을 매 문제 적용 | 최종 합계 아님 | 판 중반의 음수 표시는 남은 문제를 포기하게 만든다 |
| `koPrompt`는 다의어에만 | 유의어에는 미적용 | 유의어는 각인이 틀리지 않는다. 괄호가 늘면 화면만 지저분해진다 |
| 사운드 3+6 하이브리드 | 9개 전부 파일 아님 | 단순 톤은 합성과 품질 차이가 없고 음정 제어가 자유롭다. 질감이 필요한 3개만 CC0 파일 |
| 환호는 골에, 차임은 점수에 | 선방 시 정답음 미재생 | 정답음을 틀면 화면과 모순되고, 부저를 틀면 학습 피드백이 오염된다 |
