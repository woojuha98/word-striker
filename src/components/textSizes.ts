/**
 * 자동 축소 글자 크기 단계 (§12.3)
 *
 * 컴포넌트 파일에서 내보내면 fast refresh가 깨지므로 따로 둔다.
 * 큰 것부터 작은 것 순서로 나열한다.
 */

/** 골대 칸 (§4.2) — 기본 20px, 넘치면 14px까지 */
export const CELL_SIZES = [20, 18, 16, 14]

/** 문제 제시어(단어) — 32~40px 범위 */
export const PROMPT_SIZES = [40, 36, 32]

/**
 * 문제 제시어(문장) — CLOZE용.
 * 단어 크기를 그대로 쓰면 한 문장이 제시 영역(§4.1의 15%)을 넘친다.
 */
export const SENTENCE_SIZES = [24, 22, 20, 18, 16]
